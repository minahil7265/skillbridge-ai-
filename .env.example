# Supabase (client-side, safe to expose)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Groq API key (server-side only — do NOT expose to the browser)
# Get yours at https://console.groq.com/keys
GROQ_API_KEY=
